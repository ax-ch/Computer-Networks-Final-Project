------------------------------------------------------------------------
ISSUE #001
------------------------------------------------------------------------
CONTEXT: 
Phase 0 - Workspace & Repository Initialization

SYMPTOM: 
During the initial 'git push' to sync the provided HTML assets, the terminal threw a warning: "File assets/osi.mp4 is 54.67 MB; this is larger than GitHub's recommended maximum... GH001: Large files detected."

ROOT CAUSE: 
GitHub has a soft limit of 50 MB per file, triggering a Git LFS (Large File Storage) warning prompt. However, the hard limit blocking uploads is 100 MB. 

RESOLUTION: 
Verified that the file push actually succeeded despite the warning. Decided to bypass Git LFS configuration to avoid overcomplicating the repository setup, as the file size (54.67 MB) remained safely under the strict 100 MB block limit.

------------------------------------------------------------------------
ISSUE #002
------------------------------------------------------------------------
CONTEXT: 
Phase 1 - Web Server Setup (TCP Socket Testing)

SYMPTOM: 
When testing the initial TCP web server at `localhost:8000` via a web browser (Firefox), the expected dummy text ("Hello from TCP Server!") flashed briefly on the screen, followed immediately by an error page stating: "localhost:8000 sent back an empty page."

ROOT CAUSE: 
1. Missing Headers: The initial socket response lacked a `Content-Length` header. Because the socket was closed immediately after sending, the browser assumed the connection crashed mid-transmission.
2. Ghost Requests: The browser automatically sent a secondary background request for `GET /favicon.ico`. The server blindly responded to this image request with the same HTML text string, confusing the browser's parser.

RESOLUTION: 
Updated the `handle_tcp_client` function in `webserver.py` to construct a fully compliant HTTP/1.1 response. Explicitly calculated and added the `Content-Length` header based on the byte size of the payload, and added a `Connection: close` header to cleanly signal the end of the transmission to the browser.
------------------------------------------------------------------------

------------------------------------------------------------------------
ISSUE #003
------------------------------------------------------------------------
CONTEXT: 
Phase 1/2 - Server Testing & Port Binding

SYMPTOM: 
When launching webserver.py, the terminal threw an OSError: "[WinError 10048] Only one usage of each socket address (protocol/network address/port) is normally permitted" specifically crashing on the udp_socket.bind() function for port 9000.

ROOT CAUSE: 
A previous execution of webserver.py was terminated abruptly. While the main thread closed, the daemonized UDP server thread remained active in the background as an orphaned process, keeping Port 9000 bound and locked.

RESOLUTION: 
Used the Windows command line tool `taskkill /F /IM python.exe /T` to force-terminate all hanging Python processes, successfully freeing up Port 9000 for the next execution.
------------------------------------------------------------------------

------------------------------------------------------------------------
ISSUE #004
------------------------------------------------------------------------
CONTEXT: 
Phase 2 - Proxy-to-Web Server Forwarding Implementation

SYMPTOM: 
When accessing the proxy via a web browser, the browser displayed an empty page. The proxy terminal logged: "[WinError 10054] An existing connection was forcibly closed by the remote host."

ROOT CAUSE: 
A buffer size mismatch. The browser sent an HTTP request larger than 1024 bytes. The Web Server (`webserver.py`) was configured with `recv(1024)`. It read the first 1024 bytes, sent the response, and closed the socket. Because unread data remained in the Windows TCP receive buffer, the OS issued an RST (Reset) packet instead of a FIN packet. This caused the Proxy's `recv()` loop to crash before forwarding the response to the client.

RESOLUTION: 
Increased the buffer size in `webserver.py` from `1024` to `4096` to ensure the entire HTTP request headers are consumed before the connection is closed.
------------------------------------------------------------------------

------------------------------------------------------------------------
ISSUE #005
------------------------------------------------------------------------
CONTEXT:
TCP Client Testing and Code Revision

SYMPTOM:
After implementing a revised version of client.py with additional response display and QoS calculations, the terminal displayed an "HTTP/1.1 404 Not Found" response during TCP testing.

ROOT CAUSE:
The TCP connection between the client, proxy, and web server was functioning correctly because the client still received a valid HTTP response. However, the revised implementation introduced a mismatch between the requested resource path and the existing server resource configuration, causing the web server to return a 404 response.

RESOLUTION:
To maintain a stable implementation for testing and demonstration, the client code was reverted to the previous working version. The original implementation successfully matched the existing proxy and web server configuration and produced the expected TCP communication flow.

------------------------------------------------------------------------
ISSUE #006
------------------------------------------------------------------------
CONTEXT:
Command Line Argument Compliance

SYMPTOM:
The initial client script relied on basic sys.argv indexing, expecting arguments like "normal" or "udp," which violated the strict "-mode tcp" and "-mode udp" constraints outlined in the Final Project Guide.

ROOT CAUSE:
The initial implementation used basic positional arguments rather than a robust parsing library capable of enforcing specific flags and optional parameters.

RESOLUTION:
Replaced sys.argv parsing with Python's native argparse library to strictly enforce the required "-mode" flags and optional "-file" arguments.

------------------------------------------------------------------------
ISSUE #007
------------------------------------------------------------------------
CONTEXT:
UDP QoS Payload Formatting

SYMPTOM:
The UDP tester was sending a basic "PING {i+1}" payload, which did not meet the exact specification required for the QoS echo server.

ROOT CAUSE:
The packet construction logic was missing the transmission start time variable and was formatting the string incorrectly based on the strict guide parameters.

RESOLUTION:
Updated the packet construction logic to include the transmission start time, formatting it exactly as "Ping <seq> <timestamp>".

------------------------------------------------------------------------
ISSUE #008
------------------------------------------------------------------------
CONTEXT:
UDP QoS Statistics Calculation

SYMPTOM:
The QoS logic successfully calculated Min/Avg/Max RTT and Packet Loss, but completely omitted the Jitter metric required for the final evaluation.

ROOT CAUSE:
The initial script draft lacked the mathematical logic required to calculate the standard deviation of consecutive RTT differences.

RESOLUTION:
Imported the built-in statistics module and implemented logic to calculate the standard deviation of consecutive RTT differences (RTT_i - RTT_i-1) using statistics.stdev().

------------------------------------------------------------------------
ISSUE #009
------------------------------------------------------------------------
CONTEXT:
UDP Socket Constraints

SYMPTOM:
The socket timeout was originally set to TIMEOUT = 2, violating the project constraint that dictates a maximum 1-second timeout per packet.

ROOT CAUSE:
Incorrect timeout constant configuration during the initial drafting phase.

RESOLUTION:
Adjusted the UDP socket configuration to udp_socket.settimeout(1.0) to ensure strict compliance with the testing parameters.

------------------------------------------------------------------------
ISSUE #010
------------------------------------------------------------------------
CONTEXT:
TCP HTTP Client Output Verification

SYMPTOM:
The TCP client was receiving data chunks and appending them to a response variable, but only printed "[CLIENT] Response received" instead of outputting the actual requested file.

ROOT CAUSE:
The script successfully accumulated the byte data but lacked a print statement to decode and output the final response payload to the standard output.

RESOLUTION:
Added print(response.decode('utf-8')) to the end of the TCP data loop to ensure the HTML is properly rendered in the terminal for grading verification.

------------------------------------------------------------------------
ISSUE #011
------------------------------------------------------------------------
CONTEXT:
Web Server Error Handling and Logging Requirements

SYMPTOM:
The server lacked handling for 500 Internal Server Errors and the terminal logs did not include timestamps, which are explicitly required by the project guide.

ROOT CAUSE:
The catch-all exception block only dropped the connection without sending an HTTP failure response, and the `print()` statements relied on basic string formatting without utilizing the `datetime` module.

RESOLUTION:
Added a `get_timestamp()` helper function using `datetime.now()` to prepend formatted times to all console logs. Expanded the generic `except Exception as e:` block to encode and send an `HTTP/1.1 500 Internal Server Error` payload before closing the socket.

------------------------------------------------------------------------
ISSUE #012
------------------------------------------------------------------------
CONTEXT:
Web Server Path Sanitization and Directory Traversal 

SYMPTOM:
The server was vulnerable to malicious directory traversal (e.g., escaping the base directory via `../../`) and could fail across different operating systems due to varying slash directions.

ROOT CAUSE:
The requested path was being constructed using raw string manipulation (stripping the leading slash) instead of utilizing the `os` module for robust path normalization.

RESOLUTION:
Implemented `os.path.abspath` and `os.path.normpath` to sanitize incoming requests against a `BASE_DIR`. To strictly comply with the guide's grading rubric without introducing unrequested error codes, directory traversal attempts were configured to cleanly trigger a `raise FileNotFoundError`, routing the malicious request directly to the standard 404 response block.

------------------------------------------------------------------------
ISSUE #013
------------------------------------------------------------------------
CONTEXT:
Web Server Grading Rubric Compliance

SYMPTOM:
The server functioned correctly, but the console output text did not strictly match the exact phrasing required by the "Testing Scenarios" grading table. 

ROOT CAUSE:
Standard, generic informational prints were used for startup and connection events instead of the specific strings the grader will be searching for.

RESOLUTION:
Updated the main execution block to print the exact expected string: "[*] Server running on port 8000/9000, thread pool ready". Additionally, added a specific log inside the TCP handler stating "Thread created for new connection..." to provide explicit proof of multithreading per the rubric's concurrent load check.
------------------------------------------------------------------------

------------------------------------------------------------------------
ISSUE #014
------------------------------------------------------------------------
CONTEXT:
Proxy Server Error Handling and Logging Requirements

SYMPTOM:
The proxy server lacked handling for 504 Gateway Timeout errors, the terminal logs did not include timestamps, and response times were not calculated, all of which are explicitly required by the project guide.

ROOT CAUSE:
The proxy code did not set a timeout on the `server_socket`, meaning it would hang indefinitely if the web server was unreachable, instead of raising a `socket.timeout` exception. Additionally, the `print()` statements relied on basic string formatting without utilizing the `datetime` module or calculating the duration using the `time` module.

RESOLUTION:
Added `server_socket.settimeout(5.0)` to enforce a timeout on the connection to the web server, allowing the script to catch `socket.timeout` and send a `504 Gateway Timeout` response. Implemented `time.time()` to calculate and log the response time for each request. Updated all `print()` statements to include a formatted timestamp using `datetime.now()`.

------------------------------------------------------------------------
ISSUE #015
------------------------------------------------------------------------
CONTEXT:
Proxy Server Cache Race Conditions

SYMPTOM:
Under concurrent multi-client load, the proxy server was vulnerable to cache corruption due to race conditions during write operations.

ROOT CAUSE:
Multiple threads could attempt to open and write to the same cache file simultaneously when processing concurrent requests for the same resource.

RESOLUTION:
Introduced a global `threading.Lock()` (`cache_lock`). The file write operation for caching the response (`with open(cache_filename, 'wb') as f:`) is now wrapped inside `with cache_lock:` to ensure that only one thread can write to the cache file at a time, preventing data corruption.

------------------------------------------------------------------------
ISSUE #016
------------------------------------------------------------------------
CONTEXT:
Proxy Server Grading Rubric Compliance

SYMPTOM:
The proxy server functioned correctly, but the console output did not exactly match the phrasing required by the "Testing Scenarios" grading table.

ROOT CAUSE:
Standard, generic informational prints were used for the startup event instead of the specific string the grader will be searching for.

RESOLUTION:
Updated the main execution block to print the exact expected string: "[*] Proxy listening on port 8080, multithreading active" to provide explicit proof of compliance per the rubric.
------------------------------------------------------------------------

------------------------------------------------------------------------
ISSUE #017
------------------------------------------------------------------------
CONTEXT:
Client QoS Metrics (Throughput Calculation)

SYMPTOM:
The client script successfully outputted RTT, packet loss, and jitter, but failed to calculate and display the network throughput during the UDP QoS testing phase.

ROOT CAUSE:
The code lacked variables to measure the total test duration and track the total byte size of the successfully received payloads, which are strictly required to compute throughput according to the project's QoS formula.

RESOLUTION:
Added `total_successful_payload` to accumulate the byte size of received echo packets (`len(data)`) and used `time.time()` to measure the `total_test_duration`. Implemented the formula `(total_successful_payload * 8) / total_test_duration` to calculate bits per second, dividing by 1000 to display the final metric in kbps.
------------------------------------------------------------------------

------------------------------------------------------------------------
ISSUE #018
------------------------------------------------------------------------
CONTEXT:
Web Server UDP Echo Logging (Optional Enhancement)

SYMPTOM:
During UDP QoS testing, the web server successfully echoed the packets back to the client, but the server's terminal remained completely silent, providing no visual confirmation of the UDP traffic being handled.

ROOT CAUSE:
The `start_udp_server()` loop correctly implemented `udp_socket.sendto()` to bounce the payload, but intentionally lacked a `print()` statement to avoid terminal clutter during rapid ping tests.

RESOLUTION:
Added an explicit `print(f"[{get_timestamp()}] [UDP] Echoed packet back to {client_address[0]}")` statement immediately before sending the UDP packet back to the client. This provides visual proof in the server terminal that the QoS pings are successfully reaching the server and being processed.
------------------------------------------------------------------------